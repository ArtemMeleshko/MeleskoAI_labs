#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    float x;
    int p;
    cout << "Enter numers and pow \n";
    cin >> x >> p;
    x = pow(x,p);
    cout << x;
    return 0;
}
