#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    int w,h;
    cout << "Enter w & h : ";
    cin >> w;
    cin >> h;
    for (int i = 1; i <= h; ++i)
    {
        for (int j = 1; j <= w; ++j)
        {

            cout<<"*";
        }
        cout << endl;
    }
    return 0;
}
